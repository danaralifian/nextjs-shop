import Config from '~/utils/Config'

const styles = {
    input : {
        borderRadius : 5, 
        padding : '0px 5px',
        margin : '0px 10px',
        width : '100%',
        backgroundColor : '#fcf7ff',
        fontFamily : Config.font,
        height :34
    }
}

export default styles